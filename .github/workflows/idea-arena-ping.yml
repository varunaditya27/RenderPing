name: IdeaArena Backend Keep Alive 🤖

on:
  schedule:
    - cron: "*/5 * * * *" # Every 5 minutes
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Curl IdeaArean Backend endpoint
        run: curl -s ${{ secrets.IDEAARENA_URL }} || echo "Failed to ping IdeaArena Backend"
