name: EcoDose Backend Keep Alive 🌿

on:
  schedule:
    - cron: '*/10 * * * *'  # Every 10 minutes
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Curl EcoDose backend endpoint
        run: curl -s ${{ secrets.ECODOSE_URL }} || echo "Failed to ping EcoDose backend"
