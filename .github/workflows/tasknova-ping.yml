name: Tasknova Bot Keep Alive 🤖

on:
  schedule:
    - cron: "*/10 * * * *" # Every 10 minutes
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Curl Tasknova bot endpoint
        run: curl -s ${{ secrets.TASKNOVA_URL }} || echo "Failed to ping Tasknova"